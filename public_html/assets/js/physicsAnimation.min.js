"use strict";

function blendColors(t, e, a) {
  var n = parseInt(t.slice(1), 16),
      i = parseInt(e.slice(1), 16),
      o = n >> 16,
      r = n >> 8 & 255,
      s = 255 & n,
      l = i >> 16,
      h = i >> 8 & 255,
      d = 255 & i;return "#" + (16777216 + 65536 * (Math.round((l - o) * a) + o) + 256 * (Math.round((h - r) * a) + r) + (Math.round((d - s) * a) + s)).toString(16).slice(1);
}function physicsAnimation(t, e, a, n, i, o, r) {
  function s() {
    p.translation.set(t.width / 2, t.height / 2), c.translation.copy(p.translation);
  }t.renderer.domElement.style.overflow = "visible";var l = a,
      h = n,
      d = o || .03,
      p = t.makeGroup(),
      c = t.makeGroup(),
      u = new Physics(),
      m = [],
      w = 0;for (w = 0; w < i; w++) {
    var f = w / i * Math.PI * 2,
        k = l * Math.cos(f),
        M = h * Math.sin(f),
        y = Math.random() * (.95 - .85) + .85,
        b = y * k,
        g = y * M,
        v = u.makeParticle(100, k, M),
        P = u.makeParticle(100 * Math.random() * .66 + 33, b, g);u.makeSpring(P, v, d, 0, 0);v.makeFixed(), P.shape = t.makeCircle(P.position.x, P.position.y, 1), P.shape.noStroke().fill = "rgba(255,255,255,0)", P.position = P.shape.translation, c.add(P.shape), m.push(P.position);
  }var S = t.makeLinearGradient(-t.width / 2, t.height / 2, t.width / 2, t.height / 2, new Two.Stop(0, blendColors(e, "#000000", .3)), new Two.Stop(1, e)),
      T = new Two.Path(m, !0, !0);T.fill = r ? S : e, T.scale = 1.15, T.linewidth = 0, p.add(T), s(), t.bind("resize", s).bind("update", function () {
    u.update();
  }).play();
}function controlAnimation(t, e) {
  setTimeout(function () {
    e && t.pause();
  }, 1e3), e || t.play();
}